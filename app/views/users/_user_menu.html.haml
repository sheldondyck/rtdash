.navbar.navbar-fixed-top
  .navbar-inner{:data => {'scollspy' => 'scollspy'}}
    .container
      = link_to image_tag 'rtd-logo-menubar.png', :class => 'brand'
      = link_to 'Real Time Dashboard', user_url, :class => 'brand'
      %ul.nav
        %li{:class => active('List')}
          = link_to 'Tickets', user_url
      %ul.nav.pull-right
        %li
          - if signed_in?
            = link_to 'Logout ' + current_user.name, logout_url, :method => :delete
          - else
            = link_to 'Login', login_url
.navbar-spacer
!= flash_handler
